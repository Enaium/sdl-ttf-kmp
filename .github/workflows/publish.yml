name: Publish

"on":
  workflow_dispatch:
    inputs:
      version:
        description: "Release version to publish (e.g. 1.0.0)"
        required: false
        default: "1.0.0"

# Publishes every artifact from the runner that can build it. Maven Central
# coordinates are immutable, so each publication is produced by exactly one
# runner:
#
#   macos-14      -> :sdl-ttf-kmp metadata + jvm + all Apple target klibs
#                    (macos/ios/tvos, each with the embedded SDL_ttf static
#                    library built on this runner) + the
#                    sdl-ttf-kmp-jni-jvm-darwin-{aarch64,x86_64} artifacts
#   ubuntu-latest -> :sdl-ttf-kmp linuxX64 + linuxArm64 + mingwX64 klibs (with
#                    the embedded static library; the latter two are
#                    cross-compiled) + sdl-ttf-kmp-jni-jvm-linux-x86_64 and
#                    sdl-ttf-kmp-jni-jvm-linux-aarch64 (cross-compiled)
#   ubuntu-latest -> :sdl-ttf-kmp androidNative klibs (arm64/arm32/x64/x86,
#                    with the embedded static library built via the Android
#                    NDK)
#   windows-latest -> sdl-ttf-kmp-jni-jvm-windows-x86_64 (native MinGW build)
#
# The kotlinMultiplatform metadata module (published from macOS) redirects to
# the per-target klib modules, so all of them must be published before
# consumers can resolve the artifact.
#
# Trigger: run manually via the Actions tab. The version is taken from the
# workflow input (-PsdlTtfVersion), so a release version must be used (Maven
# Central does not accept SNAPSHOTs).
#
# Required repository secrets (Settings -> Secrets and variables -> Actions):
#   MAVEN_CENTRAL_USERNAME   Sonatype Central Portal user-token username
#   MAVEN_CENTRAL_PASSWORD   Sonatype Central Portal user-token password
#   SIGNING_KEY              base64-encoded PGP secret key ring (.gpg, binary)
#   SIGNING_KEY_ID           8-hex-digit key id (without the 0x prefix)
#   SIGNING_PASSWORD         Passphrase of the signing key

jobs:
  publish-macos:
    name: Publish macOS (metadata + jvm + Apple klibs)
    runs-on: macos-14
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Configure JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          # The SDL/SDL_ttf submodules ship their own wrapper jars, which are
          # not part of the action's known-good checksum list.
          validate-wrappers: false

      - name: Decode signing key
        run: |
          echo "${{ secrets.SIGNING_KEY }}" | tr -d ' \n' | base64 -d > "$RUNNER_TEMP/signing-key.gpg"
          ls -la "$RUNNER_TEMP/signing-key.gpg"
          head -c 16 "$RUNNER_TEMP/signing-key.gpg" | od -c | head -2

      # Per-target publications are used instead of
      # publishAllPublicationsToMavenCentralRepository so that the linuxX64 /
      # mingwX64 klibs (built with their embedded static library on the Linux
      # runner) are not published from here as bindings-only klibs.
      - name: Publish metadata, jvm, Apple target klibs and the darwin JNI artifacts
        run: >
          ./gradlew --no-daemon --stacktrace
          -PsdlTtfVersion="${{ inputs.version }}"
          -Psigning.keyId="${{ secrets.SIGNING_KEY_ID }}"
          -Psigning.secretKeyRingFile="${{ runner.temp }}/signing-key.gpg"
          -Psigning.password="${{ secrets.SIGNING_PASSWORD }}"
          :sdl-ttf-kmp:publishKotlinMultiplatformPublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishJvmPublicationToMavenCentralRepository
          :ttf-jni-jvm-darwin-aarch64:publishMavenPublicationToMavenCentralRepository
          :ttf-jni-jvm-darwin-x86_64:publishMavenPublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishMacosArm64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishMacosX64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishIosArm64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishIosX64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishIosSimulatorArm64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishTvosArm64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishTvosSimulatorArm64PublicationToMavenCentralRepository
          --continue
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}

  publish-linux:
    name: Publish Linux (linuxX64/linuxArm64/mingwX64 klibs)
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Configure JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64 \
            gcc-aarch64-linux-gnu g++-aarch64-linux-gnu

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          # The SDL/SDL_ttf submodules ship their own wrapper jars, which are
          # not part of the action's known-good checksum list.
          validate-wrappers: false

      - name: Decode signing key
        run: |
          echo "${{ secrets.SIGNING_KEY }}" | tr -d ' \n' | base64 -d > "$RUNNER_TEMP/signing-key.gpg"
          ls -la "$RUNNER_TEMP/signing-key.gpg"
          head -c 16 "$RUNNER_TEMP/signing-key.gpg" | od -c | head -2

      - name: Publish linuxX64/linuxArm64/mingwX64 klibs and the linux JNI artifacts
        run: >
          ./gradlew --no-daemon --stacktrace
          -PsdlTtfVersion="${{ inputs.version }}"
          -Psigning.keyId="${{ secrets.SIGNING_KEY_ID }}"
          -Psigning.secretKeyRingFile="${{ runner.temp }}/signing-key.gpg"
          -Psigning.password="${{ secrets.SIGNING_PASSWORD }}"
          :sdl-ttf-kmp:publishLinuxX64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishLinuxArm64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishMingwX64PublicationToMavenCentralRepository
          :ttf-jni-jvm-linux-x86_64:publishMavenPublicationToMavenCentralRepository
          :ttf-jni-jvm-linux-aarch64:publishMavenPublicationToMavenCentralRepository
          # The windows-x86_64 JNI artifact is published by the
          # publish-windows job (native MinGW build), not from here.
          --continue
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}

  publish-android:
    name: Publish Android (androidNative klibs)
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Configure JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      # The androidNative klibs embed the SDL_ttf static library, which is
      # cross-compiled with the Android NDK.
      - name: Install Android NDK
        run: |
          yes | ${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager --install "ndk;27.0.12077973" >/dev/null

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          # The SDL/SDL_ttf submodules ship their own wrapper jars, which are
          # not part of the action's known-good checksum list.
          validate-wrappers: false

      - name: Decode signing key
        run: |
          echo "${{ secrets.SIGNING_KEY }}" | tr -d ' \n' | base64 -d > "$RUNNER_TEMP/signing-key.gpg"
          ls -la "$RUNNER_TEMP/signing-key.gpg"
          head -c 16 "$RUNNER_TEMP/signing-key.gpg" | od -c | head -2

      - name: Publish androidNative klibs
        run: >
          ./gradlew --no-daemon --stacktrace
          -PsdlTtfVersion="${{ inputs.version }}"
          -Psigning.keyId="${{ secrets.SIGNING_KEY_ID }}"
          -Psigning.secretKeyRingFile="${{ runner.temp }}/signing-key.gpg"
          -Psigning.password="${{ secrets.SIGNING_PASSWORD }}"
          :sdl-ttf-kmp:publishAndroidNativeArm64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishAndroidNativeArm32PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishAndroidNativeX64PublicationToMavenCentralRepository
          :sdl-ttf-kmp:publishAndroidNativeX86PublicationToMavenCentralRepository
          --continue
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}

  publish-windows:
    name: Publish Windows (windows-x86_64 JNI artifact)
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Configure JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: Install CMake
        shell: pwsh
        run: |
          choco install cmake -y --no-progress
          # cmake.exe may not be on PATH for the Gradle daemon.
          echo "C:\Program Files\CMake\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Install MinGW
        shell: pwsh
        run: |
          choco install mingw -y --no-progress
          # The JVM needs MinGW's runtime DLLs (libwinpthread-1.dll,
          # libgcc_s_seh-1.dll) on PATH to load the JNI DLL.
          echo "C:\ProgramData\chocolatey\lib\mingw\tools\install\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          # The SDL/SDL_ttf submodules ship their own wrapper jars, which are
          # not part of the action's known-good checksum list.
          validate-wrappers: false

      - name: Decode signing key
        shell: pwsh
        run: |
          [IO.File]::WriteAllBytes(
            "$env:RUNNER_TEMP\signing-key.gpg",
            [Convert]::FromBase64String("${{ secrets.SIGNING_KEY }}")
          )
          Get-Item "$env:RUNNER_TEMP\signing-key.gpg" | Select-Object Length

      - name: Publish the windows-x86_64 JNI artifact
        shell: cmd
        run: >
          ./gradlew --no-daemon --stacktrace
          -PsdlTtfVersion="${{ inputs.version }}"
          -Psigning.keyId="${{ secrets.SIGNING_KEY_ID }}"
          -Psigning.secretKeyRingFile="${{ runner.temp }}\signing-key.gpg"
          -Psigning.password="${{ secrets.SIGNING_PASSWORD }}"
          :ttf-jni-jvm-windows-x86_64:publishMavenPublicationToMavenCentralRepository
          --continue
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
